const h = require('./helpers.js');
const crud = require('./crud.js');


// searches by tag
// returns only if tag matches string

// search function takes object, returns keys in db objects that match
// tags need another filter within the array of tags


// sort todos
// sorts by criterium (date, createddate, duedate)

module.exports = () => {};
